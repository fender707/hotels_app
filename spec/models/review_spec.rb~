require 'spec_helper'

RSpec.describe Review, type: :model do

  it { is_expected.to belongs_to(:hotel) }

  it { is_expected.to validate_persence_of(:title) }

  it { is_expected.to validate_persence_of(:rating) }

  it { is_expected.to validate_persence_of(:comment) }

end
